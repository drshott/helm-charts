apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.name }}
  labels:
    app.kebernetes.io/name: {{ .Values.global.name }}
    app.kubernetes.io/managed-by: 'helm'
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.gistApiDeployment.containerPort }}
      protocol: TCP
      name: http
  selector:
    app.kebernetes.io/name: {{ .Values.global.name }}
